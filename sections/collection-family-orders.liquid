{% paginate collection.products by 24 %}
<section id="FamilyOrdersCollection" aria-label="Family Orders Menu">
  <div class="container">
    <div class="collection-header animate-on-scroll">
      <div class="collection-badge preorder" aria-label="Pre-order required">
        <svg width="8" height="8" viewBox="0 0 8 8" aria-hidden="true"><circle cx="4" cy="4" r="4" fill="currentColor"/></svg>
        Pre-order
      </div>
      <h1 style="margin-bottom: 10px;">{{ collection.title | default: 'Family Feast — Pre-orders' }}</h1>
      {% if collection.description != blank %}
        <p style="max-width:600px;">{{ collection.description }}</p>
      {% else %}
        <p style="max-width:600px;">Feeding the family? Browse our feast selections and place a pre-order inquiry. We'll confirm and send you a payment link.</p>
      {% endif %}
    </div>

    <!-- Products Grid (horizontal layout, redirects to family-feast) -->
    <div class="collection-grid" id="FamilyOrdersGrid">
      {% for product in collection.products %}
        {%- assign veg_meta = product.metafields.custom.is_veg -%}
        {%- assign is_veg = false -%}
        {%- if veg_meta == true or veg_meta == 'true' or veg_meta == '1' -%}
          {%- assign is_veg = true -%}
        {%- endif -%}

        {%- assign tag_label = '' -%}
        {%- for tag in product.tags -%}
          {%- assign tl = tag | downcase -%}
          {%- if tl contains 'popular' -%}{%- assign tag_label = 'Popular' -%}
          {%- elsif tl contains 'must try' -%}{%- assign tag_label = 'Must Try' -%}
          {%- elsif tl contains 'best seller' -%}{%- assign tag_label = 'Best Seller' -%}
          {%- elsif tl contains "chef's special" -%}{%- assign tag_label = "Chef's Special" -%}
          {%- elsif tl contains 'new' -%}{%- assign tag_label = 'New' -%}
          {%- endif -%}
        {%- endfor -%}

        <!-- Entire card links to /pages/family-feast -->
        <a
          href="/pages/family-feast"
          class="product-card product-card--horizontal animate-on-scroll"
          id="product-family-{{ product.id }}"
          style="text-decoration:none;"
          aria-label="Pre-order {{ product.title | escape }} — click to inquire"
        >
          <!-- Text content (left) -->
          <div class="product-card__body">
            <div style="display:flex;align-items:center;gap:8px;margin-bottom:8px;">
              <span class="product-card__veg-indicator {% if is_veg %}veg{% else %}non-veg{% endif %}" style="position:static;" aria-label="{% if is_veg %}Vegetarian{% else %}Non-Vegetarian{% endif %}"></span>
              {% if tag_label != blank %}
                <span class="product-card__tag" style="position:static;">{{ tag_label }}</span>
              {% endif %}
            </div>
            <h3 class="product-card__name">{{ product.title }}</h3>
            {% if product.description != blank %}
              <p class="product-card__desc">{{ product.description | strip_html | truncate: 90 }}</p>
            {% endif %}

            {%- assign rating = product.metafields.reviews.rating.value -%}
            {%- if rating != blank -%}
              <div class="product-card__rating">
                <span class="product-card__stars">★★★★★</span>
                <span>{{ rating | round: 1 }}</span>
              </div>
            {%- endif -%}

            <div class="product-card__footer">
              <div class="product-card__price">
                <span class="currency">₹</span>{{ product.price | money_without_currency | remove: '.00' }}
                {% if product.compare_at_price > product.price %}
                  <del style="font-size:0.75rem;color:var(--color-text-secondary);font-weight:400;margin-left:4px;">₹{{ product.compare_at_price | money_without_currency | remove: '.00' }}</del>
                {% endif %}
              </div>

              <!-- No Add to Cart — redirect to family feast page -->
              <span
                style="display:inline-flex;align-items:center;gap:6px;padding:10px 16px;background:linear-gradient(135deg,var(--color-primary) 0%,var(--color-primary-dark) 100%);color:#111;font-size:0.8125rem;font-weight:700;font-family:var(--font-body);min-height:44px;white-space:nowrap;"
                aria-hidden="true"
              >
                Pre-order
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="12" height="12"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
              </span>
            </div>
          </div>

          <!-- Image (right, 150×150) -->
          <div class="product-card__image-wrap" style="max-width:150px;min-width:150px;">
            {% if product.featured_image %}
              <img
                src="{{ product.featured_image | image_url: width: 300 }}"
                alt="{{ product.featured_image.alt | default: product.title | escape }}"
                width="150"
                height="150"
                loading="lazy"
                style="object-fit:cover;width:100%;height:100%;"
              >
            {% else %}
              <div style="width:100%;height:100%;display:flex;align-items:center;justify-content:center;font-size:2.5rem;background:var(--color-surface-elevated);">🍚</div>
            {% endif %}
          </div>
        </a>

      {% else %}
        <div style="text-align:center;padding:60px 0;color:var(--color-text-secondary);">
          <p>No family feast items available yet. Check back soon!</p>
        </div>
      {% endfor %}
    </div>

    {% if paginate.pages > 1 %}
      <div style="display:flex;justify-content:center;gap:8px;margin:40px 0;">
        {{ paginate | default_pagination }}
      </div>
    {% endif %}
  </div>
</section>
{% endpaginate %}

{% schema %}
{
  "name": "Family Orders Collection",
  "settings": []
}
{% endschema %}
